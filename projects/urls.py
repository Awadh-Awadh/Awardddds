from django.urls import path
from . import views


urlpatterns = [
  # path('',views.projects, name = 'profile'),
#   path('projects/',views.all-projects, name = 'all'),
#   path('prjectDetail/',views.detail, name = 'project-detail'),
#   path('project-create/',views.upload,name='upload')
 ]